{
  "name": "pr-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "cd learnify && npm run dev",
    "start": "cd learnify && npm run dev",
    "build": "cd learnify && npm run build",
    "install": "cd learnify && npm install"
  }
}
